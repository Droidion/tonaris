[build]
builder = "RAILPACK"
buildCommand = "go build -o ./out/server ./cmd/server"

[deploy]
startCommand = "./out/server"
healthcheckPath = "/healthz"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
